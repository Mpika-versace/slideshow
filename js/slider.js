//global variable
const elements=document.querySelector('#elements');
let slider=elements.children;
let fromSlider=Array.from(slider);
console.log(slider);
console.log(fromSlider);

export {elements};